
import { isNumeric } from 'mi-guatemala/helpers/is-numeric';
import { module, test } from 'qunit';

module('Unit | Helper | is numeric');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNumeric([42]);
  assert.ok(result);
});

